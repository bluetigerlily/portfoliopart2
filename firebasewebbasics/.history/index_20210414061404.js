var mainText = document.getElementById("mainText");
var submitBtn = document.getElementById("submitBtn");
var database = firebase.database();
function submitClick(usedId, name,) {
   
    firebase.database().ref('users/' + userId).set({

    })




}